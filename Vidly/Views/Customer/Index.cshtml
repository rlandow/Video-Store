@model IEnumerable<Vidly.Models.Customer>

<!-- <p>
    @Html.ActionLink("Create New", "Create")
</p>
    -->
@if (Model.Count() == 0)
{
    <text> No hits</text>
}
else
{
<ul style="padding-left:0">
    <li style="display:inline; margin-right:20px">@Html.ActionLink("Home", "Index", "Home")</li>
    <li style="display:inline; margin-right:20px">@Html.ActionLink("Movies", "List", "Movies")</li>
</ul>
    <text>
        <h2>Customers</h2>
        @Html.ActionLink("New Customer", "New", "Customer", null, new { @class="btn btn-primary"})
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>Memberhip Type</th>
                <th>Discount Rate</th>

            </tr>
    </text>

    foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.ActionLink(item.Name, "Edit", "Customer", new { id = item.Id }, null)
            </td>
            <td>
                @item.MembershipType.Name
            </td>
            <td>
                @item.MembershipType.DiscountRate%
            </td>
            <!--<td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>-->
        </tr>
    }
}

</table>
