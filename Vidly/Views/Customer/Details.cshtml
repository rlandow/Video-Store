@model Vidly.Models.Customer

<br />
<h2>@Model.Name</h2>
<ul>
   
        <li>
            Membership Type: @Model.MembershipType.Name
        </li>
        @if (Model.Birthdate != null)
        {
        <li>
            DOB: @string.Format("{0:dd/MM/yyyy}", Model.Birthdate)
            
        </li>
        }
</ul>